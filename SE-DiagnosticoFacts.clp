;; nvlExp = Alta - Enfermera
(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla KN-95)))


(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla Tela)))


(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla Tela)))


(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Alta)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla Tela)))

;; nvlExp = Media - Secretaria

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla KN-95)))


(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla Tela)))


(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla Tela)))


(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Media)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla Tela)))

;; nvlExp = Baja - Estudiante

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Alta)
        (tos Normal)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Alta)
        (tos Seca)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla KN-95)))


(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Media)
        (tos Normal)
        (tipoMascarrilla Tela)))


(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Media)
        (tos Seca)
        (tipoMascarrilla Tela)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Baja)
        (tos Normal)
        (tipoMascarrilla Tela)))


(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla KN-95)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla Quirurgica)))

(assert
    (diagnostico-covid
        (nvlExp Baja)
        (fiebre Baja)
        (tos Seca)
        (tipoMascarrilla Tela)))